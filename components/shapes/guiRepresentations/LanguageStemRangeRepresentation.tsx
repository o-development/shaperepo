import RepresenationComponent from './RepresenationComponent';
import { LanguageStemRange } from '../../../types/shexTypes';

const LanguageStemRangeRepresentation: RepresenationComponent<LanguageStemRange> = () => {
  return null;
};

export default LanguageStemRangeRepresentation;
