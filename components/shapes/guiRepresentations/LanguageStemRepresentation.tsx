import RepresenationComponent from './RepresenationComponent';
import { LanguageStem } from '../../../types/shexTypes';

const LanguageStemRepresentation: RepresenationComponent<LanguageStem> = () => {
  return null;
};

export default LanguageStemRepresentation;
