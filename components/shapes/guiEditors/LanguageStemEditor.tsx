import EditorComponent from './EditorComponent';
import { LanguageStem } from '../../../types/shexTypes';

const LanguageStemEditor: EditorComponent<LanguageStem> = () => {
  return null;
};

export default LanguageStemEditor;
