import EditorComponent from './EditorComponent';
import { LanguageStemRange } from '../../../types/shexTypes';

const LanguageStemRangeEditor: EditorComponent<LanguageStemRange> = () => {
  return null;
};

export default LanguageStemRangeEditor;
