import EditorComponent from './EditorComponent';
import { shapeExpr } from '../../../types/shexTypes';

const StartEditor: EditorComponent<shapeExpr> = () => {
  return null;
};

export default StartEditor;
