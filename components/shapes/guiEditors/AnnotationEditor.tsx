import EditorComponent from './EditorComponent';
import { Annotation } from '../../../types/shexTypes';

const AnnotationEditor: EditorComponent<Annotation> = () => {
  return null;
};

export default AnnotationEditor;
